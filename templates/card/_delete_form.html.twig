<form method="post" action="{{ path('card_delete', {'id': card.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer cette card ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ card.id) }}">
    <button class="btn">Supprimer</button>
</form>
