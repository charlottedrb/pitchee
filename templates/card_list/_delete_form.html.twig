<form method="post" action="{{ path('card_list_delete', {'id': card_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ card_list.id) }}">
    <button class="btn">Supprimer</button>
</form>
